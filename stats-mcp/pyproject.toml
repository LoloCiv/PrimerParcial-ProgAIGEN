[project]
name = "stats-mcp"
version = "1.0.0"
description = "Read-only MCP server with win/loss statistics from the turn-based games database"
requires-python = ">=3.12"
dependencies = [
    "mcp>=2.2,<3",
]
